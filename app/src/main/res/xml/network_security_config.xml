<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Blokir semua cleartext (HTTP) traffic, hanya izinkan HTTPS -->
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <!-- Percayai CA sistem saja (bukan user-installed certificate) -->
            <certificates src="system"/>
        </trust-anchors>
    </base-config>

    <!-- Firebase Realtime Database domain -->
    <domain-config cleartextTrafficPermitted="false">
        <domain includeSubdomains="true">firebasedatabase.app</domain>
        <domain includeSubdomains="true">googleapis.com</domain>
        <domain includeSubdomains="true">firebase.com</domain>
        <domain includeSubdomains="true">google.com</domain>
    </domain-config>
</network-security-config>
